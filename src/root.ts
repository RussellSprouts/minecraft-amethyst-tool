/**
 * This file intentionally left blank.
 * 
 * This file serves as the entry point for 'root' chunk,
 * which is the base for both the browser code and the web workers.
 * JSCompiler will automatically put code that is needed by both
 * sides into this chunk. It will be executed separately in both
 * contexts, so be careful about side-effects for imports that are
 * included in the worker.
 */
export { };